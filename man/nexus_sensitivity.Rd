% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nexus_advanced.R
\name{nexus_sensitivity}
\alias{nexus_sensitivity}
\title{Nexus Weight Sensitivity Analysis}
\usage{
nexus_sensitivity(
  water_score,
  energy_score,
  food_score,
  nutrient_score,
  carbon_score,
  treatment_names = NULL,
  steps = 20,
  verbose = TRUE
)
}
\arguments{
\item{water_score}{Numeric vector. Water scores (0 to 1).}

\item{energy_score}{Numeric vector. Energy scores (0 to 1).}

\item{food_score}{Numeric vector. Food scores (0 to 1).}

\item{nutrient_score}{Numeric vector. Nutrient scores (0 to 1).}

\item{carbon_score}{Numeric vector. Carbon scores (0 to 1).}

\item{treatment_names}{Character vector. Treatment labels.}

\item{steps}{Integer. Number of weight steps to evaluate.
Default 20.}

\item{verbose}{Logical. If \code{TRUE}, prints a message.
Default \code{TRUE}.}
}
\value{
A data frame with columns: dimension, weight, treatment,
  and nexus_index showing how the index changes as each dimension's
  weight varies.
}
\description{
Evaluates how the composite nexus index changes as the weight of one
dimension varies from 0 to its maximum feasible value, with remaining
weight distributed equally among the other four dimensions.
}
\examples{
\donttest{
sa <- nexus_sensitivity(
  water_score = c(0.9, 0.5), energy_score = c(0.6, 0.8),
  food_score = c(0.8, 0.7), nutrient_score = c(0.7, 0.6),
  carbon_score = c(0.5, 0.9),
  treatment_names = c("CA", "CT"), steps = 10
)
head(sa)
}

}
