% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nexus.R
\name{normalize_minmax}
\alias{normalize_minmax}
\title{Normalize Data (Min-Max)}
\usage{
normalize_minmax(x, inverse = FALSE)
}
\arguments{
\item{x}{Numeric vector.}

\item{inverse}{Logical. If \code{TRUE}, higher original values receive
lower normalized scores. Use for metrics where lower is better,
such as carbon footprint or energy intensity. Default \code{FALSE}.}
}
\value{
Numeric vector normalized to the 0 to 1 range. Returns 0.5
  for all elements if the input has zero range.
}
\description{
Min-max normalization to scale values between 0 and 1.
}
\details{
Formula: \deqn{X_{norm} = \frac{X - X_{min}}{X_{max} - X_{min}}}{X_norm = (X - X_min) / (X_max - X_min)}
}
\examples{
normalize_minmax(c(10, 20, 30, 40, 50))
normalize_minmax(c(10, 20, 30, 40, 50), inverse = TRUE)

}
