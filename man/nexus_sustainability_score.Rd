% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nexus.R
\name{nexus_sustainability_score}
\alias{nexus_sustainability_score}
\title{Nexus Sustainability Score}
\usage{
nexus_sustainability_score(
  water_score,
  energy_score,
  food_score,
  nutrient_score,
  carbon_score,
  weights = rep(0.2, 5),
  thresholds = c(0.4, 0.6, 0.8),
  verbose = TRUE
)
}
\arguments{
\item{water_score}{Numeric vector. Water dimension score (0 to 1).}

\item{energy_score}{Numeric vector. Energy dimension score (0 to 1).}

\item{food_score}{Numeric vector. Food dimension score (0 to 1).}

\item{nutrient_score}{Numeric vector. Nutrient dimension score (0 to 1).}

\item{carbon_score}{Numeric vector. Carbon dimension score (0 to 1).}

\item{weights}{Numeric vector of length 5. Default equal weights.}

\item{thresholds}{Numeric vector of length 3. Breakpoints for
sustainability categories: c(low, medium, high). Default
c(0.4, 0.6, 0.8).}

\item{verbose}{Logical. If \code{TRUE}, prints informational messages.
Default \code{TRUE}.}
}
\value{
A data frame with columns:
\describe{
  \item{nexus_score}{Numeric. Composite sustainability score (0 to 1)}
  \item{category}{Character. Sustainability category: Unsustainable,
    Low Sustainability, Moderately Sustainable, or Highly Sustainable}
  \item{water}{Numeric. Water dimension input score}
  \item{energy}{Numeric. Energy dimension input score}
  \item{food}{Numeric. Food dimension input score}
  \item{nutrient}{Numeric. Nutrient dimension input score}
  \item{carbon}{Numeric. Carbon dimension input score}
}
}
\description{
Comprehensive sustainability score with categorization based on
user-defined thresholds.
}
\details{
Formula: \deqn{NSS = \frac{1}{n} \sum_{i=1}^{n} S_i}{NSS = mean(S_i)} where S_i are normalized sub-dimension scores.
}
\examples{
\donttest{
nexus_sustainability_score(
  water_score = c(0.85, 0.60, 0.45),
  energy_score = c(0.70, 0.55, 0.35),
  food_score = c(0.90, 0.80, 0.50),
  nutrient_score = c(0.65, 0.70, 0.40),
  carbon_score = c(0.80, 0.50, 0.30)
)

}
}
