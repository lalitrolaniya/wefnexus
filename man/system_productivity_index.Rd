% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/food.R
\name{system_productivity_index}
\alias{system_productivity_index}
\title{System Productivity Index (SPI)}
\usage{
system_productivity_index(yields, prices, base_price, verbose = TRUE)
}
\arguments{
\item{yields}{Numeric vector. Yields of individual crops in the
system (kg/ha).}

\item{prices}{Numeric vector. Market prices of individual crops
(currency/kg). Must be the same length as \code{yields}.}

\item{base_price}{Numeric. Price of the base or reference crop
(currency/kg).}

\item{verbose}{Logical. If \code{TRUE}, prints informational messages.
Default \code{TRUE}.}
}
\value{
Numeric. System productivity expressed as base crop equivalent
  yield (kg/ha).
}
\description{
Converts yields from a cropping system to a common crop-equivalent
yield based on economic value.
}
\details{
\deqn{SPI = \frac{\sum(Y_i \times P_i)}{P_{base}}}{SPI = sum(Yi * Pi) / P_base}
}
\examples{
system_productivity_index(yields = c(5000, 4500),
                          prices = c(22, 25), base_price = 25)

}
