% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nexus.R
\name{nexus_index}
\alias{nexus_index}
\title{WEFNC Nexus Index}
\usage{
nexus_index(
  water_score,
  energy_score,
  food_score,
  nutrient_score,
  carbon_score,
  weights = rep(0.2, 5),
  verbose = TRUE
)
}
\arguments{
\item{water_score}{Numeric vector. Water performance score (0 to 1).}

\item{energy_score}{Numeric vector. Energy performance score (0 to 1).}

\item{food_score}{Numeric vector. Food productivity score (0 to 1).}

\item{nutrient_score}{Numeric vector. Nutrient use efficiency score
(0 to 1).}

\item{carbon_score}{Numeric vector. Carbon performance score (0 to 1),
where higher values indicate lower carbon footprint.}

\item{weights}{Numeric vector of length 5. Weights for Water, Energy,
Food, Nutrient, and Carbon dimensions. Must sum to 1. Default is
equal weighting (0.2 each).}

\item{verbose}{Logical. If \code{TRUE}, prints informational messages.
Default \code{TRUE}.}
}
\value{
Numeric vector. Composite nexus index on a 0 to 1 scale,
  where higher values indicate better overall sustainability.
}
\description{
Computes a weighted composite Water-Energy-Food-Nutrient-Carbon
nexus sustainability index from normalized dimension scores.
}
\details{
\deqn{NI = w_W S_W + w_E S_E + w_F S_F + w_N S_N + w_C S_C}
}
\examples{
\donttest{
nexus_index(water_score = c(0.85, 0.72), energy_score = c(0.70, 0.80),
            food_score = c(0.90, 0.85), nutrient_score = c(0.65, 0.70),
            carbon_score = c(0.80, 0.60))

}
}
